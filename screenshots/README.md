Add screenshots here demonstrating:

- CI/CD pipeline passing (security scans, tests, deploy)
- Grafana dashboards with metrics
- Prometheus targets and alerts
- Docker Compose services running (`docker compose ps`)
- Blue-Green deployment output
- Rollback execution
- Health check logs
- Environment validation output

These can be captured after running `bash scripts/setup.sh` or `.\scripts\setup.ps1`.
